@model tester1.Models.DonHang
@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    #content {
        background: #fff;
        height: 100%;
    }

    .card {
        margin: 20px;
    }

    .card-header {
        background-color: #f5f5f5;
        padding: 10px 20px;
    }

    .card-title {
        margin: 0;
        font-size: 24px;
        color: #333;
    }

    .card-body {
        padding: 20px;
    }

    .row {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        margin-bottom: 20px;
    }

    .col-12 {
        width: 100%;
    }

    h2 {
        margin: 0;
        font-size: 20px;
        color: #333;
        margin-bottom: 10px;
    }

    .card-footer {
        background-color: #f5f5f5;
        padding: 10px 20px;
    }
</style>
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Chi tiết đơn hàng</h3>
    </div>
    <div class="card-body">
        <div class="row" style="flex-direction: column;">
            <div class="col-12">
                <h2>Danh sách sản phẩm</h2>
            </div>
            <div class="col-12">
                @*@if (Model.TrangThaiDonHang == 5) // Giả sử TrangThai của đơn hàng được lưu trong Model.TrangThai
                    {
                        <a href="@Url.Action("CheckReview", "Rating", new { MaDH = Model.MaDH })" class="btn btn-primary">Đánh giá đơn hàng</a>
                    }*@
                @Html.Action("Partial_SanPham", "User", new { id = Model.MaDH })
            </div>
        </div>
    </div>
    <!-- /.card-body -->
    <div class="card-footer">
        <a style="width:150px" href="/User/Index" class="btn btn-primary">Trở về</a>
    </div>

</div>
